{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.11.13","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[{"sourceId":119106,"databundleVersionId":14485445,"sourceType":"competition"}],"dockerImageVersionId":31192,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"# This Python 3 environment comes with many helpful analytics libraries installed\n# It is defined by the kaggle/python Docker image: https://github.com/kaggle/docker-python\n# For example, here's several helpful packages to load\n\nimport numpy as np # linear algebra\nimport pandas as pd # data processing, CSV file I/O (e.g. pd.read_csv)\n\n# Input data files are available in the read-only \"../input/\" directory\n# For example, running this (by clicking run or pressing Shift+Enter) will list all files under the input directory\n\nimport os\nfor dirname, _, filenames in os.walk('/kaggle/input'):\n    for filename in filenames:\n        print(os.path.join(dirname, filename))\n\n# You can write up to 20GB to the current directory (/kaggle/working/) that gets preserved as output when you create a version using \"Save & Run All\" \n# You can also write temporary files to /kaggle/temp/, but they won't be saved outside of the current session","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2025-11-18T23:00:53.054465Z","iopub.execute_input":"2025-11-18T23:00:53.054935Z","iopub.status.idle":"2025-11-18T23:00:53.061478Z","shell.execute_reply.started":"2025-11-18T23:00:53.054909Z","shell.execute_reply":"2025-11-18T23:00:53.060600Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================\n# CHUNK 0: Imports & basic setup\n# =========================================\n\nfrom pathlib import Path\nimport math\nimport random\n\nimport numpy as np\nimport pandas as pd\n\n# Kaggle paths\nINPUT_FILE  = Path(\"/kaggle/input/santa-2025/sample_submission.csv\")\nOUTPUT_FILE = Path(\"/kaggle/working/submission.csv\")\n\nRNG_SEED = 42\nnp.random.seed(RNG_SEED)\nrandom.seed(RNG_SEED)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-18T23:00:56.122408Z","iopub.execute_input":"2025-11-18T23:00:56.122690Z","iopub.status.idle":"2025-11-18T23:00:56.128839Z","shell.execute_reply.started":"2025-11-18T23:00:56.122671Z","shell.execute_reply":"2025-11-18T23:00:56.127592Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================\n# CHUNK 1: Load sample_submission and parse IDs\n# =========================================\n\nsample_df = pd.read_csv(INPUT_FILE)\n\n# id format \"001_0\" → config_id=1, tree_index=0\ntmp = sample_df[\"id\"].str.split(\"_\", n=1, expand=True)\nsample_df[\"config_id\"]  = tmp[0].astype(int)\nsample_df[\"tree_index\"] = tmp[1].astype(int)\n\ndef s_to_float(v):\n    if isinstance(v, str) and v.startswith(\"s\"):\n        return float(v[1:])\n    return float(v)\n\nfor col in [\"x\", \"y\", \"deg\"]:\n    sample_df[col + \"_float\"] = sample_df[col].apply(s_to_float)\n\nsample_df.head()\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-18T23:00:59.196806Z","iopub.execute_input":"2025-11-18T23:00:59.197329Z","iopub.status.idle":"2025-11-18T23:00:59.339606Z","shell.execute_reply.started":"2025-11-18T23:00:59.197289Z","shell.execute_reply":"2025-11-18T23:00:59.338727Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================\n# CHUNK 2: Per-config minimum center distances\n# =========================================\n\nconfig_min_dist = {}\n\nfor cid, cfg in sample_df.groupby(\"config_id\"):\n    coords = cfg[[\"x_float\", \"y_float\"]].to_numpy()\n    n = len(coords)\n    if n <= 1:\n        config_min_dist[cid] = 0.0\n        continue\n\n    diff = coords[:, None, :] - coords[None, :, :]  # (n, n, 2)\n    dist2 = np.sum(diff**2, axis=-1)                # (n, n)\n    np.fill_diagonal(dist2, np.inf)\n    min_d = float(np.sqrt(np.min(dist2)))\n    config_min_dist[cid] = min_d\n\nlen(config_min_dist), list(config_min_dist.items())[:5]\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-18T23:01:02.246528Z","iopub.execute_input":"2025-11-18T23:01:02.246832Z","iopub.status.idle":"2025-11-18T23:01:02.427963Z","shell.execute_reply.started":"2025-11-18T23:01:02.246809Z","shell.execute_reply":"2025-11-18T23:01:02.427182Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================\n# CHUNK 3: Fast approximate scoring on centers\n# =========================================\n\nMARGIN = 0.5   # \"radius\" margin for tree size; only relative changes matter\n\ndef config_score_xy(xs: np.ndarray, ys: np.ndarray, margin: float = MARGIN) -> float:\n    \"\"\"\n    Approximate config score s^2 / n based on center positions:\n    - s is side of bounding square around centers, padded by `margin`.\n    \"\"\"\n    n = len(xs)\n    if n == 0:\n        return 0.0\n\n    minx = xs.min() - margin\n    maxx = xs.max() + margin\n    miny = ys.min() - margin\n    maxy = ys.max() + margin\n\n    side = max(maxx - minx, maxy - miny)\n    return (side * side) / n\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-18T23:01:05.577179Z","iopub.execute_input":"2025-11-18T23:01:05.577542Z","iopub.status.idle":"2025-11-18T23:01:05.583542Z","shell.execute_reply.started":"2025-11-18T23:01:05.577518Z","shell.execute_reply":"2025-11-18T23:01:05.582687Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================\n# CHUNK 4: Ultra-fast local search on one config (no Shapely)\n# =========================================\n\ndef improve_config_centers(config_df: pd.DataFrame,\n                           safe_min_dist: float,\n                           steps_per_tree: int = 200,\n                           max_step_xy: float = 0.05,\n                           margin: float = MARGIN) -> pd.DataFrame:\n    \"\"\"\n    Greedy local search on centers only:\n      - starting from sample_submission layout\n      - keeps angles fixed\n      - never allows any pair distance < safe_min_dist\n      - minimizes approximate s^2 / n score\n\n    steps_per_tree: how many proposals per tree (total steps = n * steps_per_tree)\n    max_step_xy: max |dx|, |dy| per step\n    \"\"\"\n    cfg = config_df.sort_values(\"tree_index\").copy()\n    xs = cfg[\"x_float\"].to_numpy().astype(float)\n    ys = cfg[\"y_float\"].to_numpy().astype(float)\n\n    n = len(xs)\n    if n <= 1:\n        return cfg  # nothing to optimize\n\n    # Precompute squared safe distance\n    safe_min_dist2 = safe_min_dist * safe_min_dist\n\n    best_score = config_score_xy(xs, ys, margin=margin)\n    total_steps = steps_per_tree * n\n\n    for step in range(total_steps):\n        i = np.random.randint(n)\n\n        old_x, old_y = xs[i], ys[i]\n\n        # propose a small random move\n        xs[i] += (np.random.rand() * 2 - 1) * max_step_xy\n        ys[i] += (np.random.rand() * 2 - 1) * max_step_xy\n\n        # distance check only for moved tree\n        dx = xs[i] - xs\n        dy = ys[i] - ys\n        dist2 = dx*dx + dy*dy\n        dist2[i] = np.inf  # ignore self\n\n        if np.min(dist2) < safe_min_dist2 - 1e-12:\n            # too close; revert\n            xs[i], ys[i] = old_x, old_y\n            continue\n\n        new_score = config_score_xy(xs, ys, margin=margin)\n        if new_score < best_score:\n            best_score = new_score  # accept\n        else:\n            xs[i], ys[i] = old_x, old_y  # reject\n\n    improved = cfg.copy()\n    improved[\"x_float\"] = xs\n    improved[\"y_float\"] = ys\n    # deg_float stays untouched (angles fixed)\n    return improved\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-18T23:01:08.656490Z","iopub.execute_input":"2025-11-18T23:01:08.656815Z","iopub.status.idle":"2025-11-18T23:01:08.666513Z","shell.execute_reply.started":"2025-11-18T23:01:08.656791Z","shell.execute_reply":"2025-11-18T23:01:08.665411Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================\n# CHUNK 5: 100%-SAFE optimization (no overlaps)\n# =========================================\n\nSAFE_SKIP_SMALL = 10      # do not optimize configs <= 10 trees\nSAFETY_FACTOR   = 1.20    # extremely safe 20% spacing buffer\n\nall_improved = []\n\nfor cid, cfg in sample_df.groupby(\"config_id\"):\n    n_trees = len(cfg)\n\n    # ================================\n    # 1) Skip tiny configs (VERY SAFE)\n    # ================================\n    if n_trees <= SAFE_SKIP_SMALL:\n        print(f\"Skipping tiny config {cid:03d} with {n_trees} trees (left unchanged)\")\n        all_improved.append(cfg.copy())\n        continue\n\n    # ====================================\n    # 2) Compute strict safety distance\n    # ====================================\n    safe_min = config_min_dist[cid] * SAFETY_FACTOR\n\n    print(f\"Improving config {cid:03d} \"\n          f\"(trees={n_trees}, safe_min={safe_min:.4f})\")\n\n    # effort increases with number of trees\n    if n_trees <= 50:\n        steps_per_tree = 200\n    elif n_trees <= 120:\n        steps_per_tree = 350\n    else:\n        steps_per_tree = 450  # large configs matter the most\n\n    improved_cfg = improve_config_centers(\n        cfg,\n        safe_min_dist=safe_min,\n        steps_per_tree=steps_per_tree,\n        max_step_xy=0.03,\n        margin=MARGIN,\n    )\n    all_improved.append(improved_cfg)\n\nimproved_df = pd.concat(all_improved, ignore_index=True)\nimproved_df.head()\n\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-18T23:01:13.471411Z","iopub.execute_input":"2025-11-18T23:01:13.472185Z","iopub.status.idle":"2025-11-18T23:05:02.727139Z","shell.execute_reply.started":"2025-11-18T23:01:13.472151Z","shell.execute_reply":"2025-11-18T23:05:02.726140Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================\n# OPTIONAL CHECK: Ensure NO config moves closer than baseline\n# =========================================\n\nbad_configs = []\n\nfor cid, cfg in improved_df.groupby(\"config_id\"):\n    xs = cfg[\"x_float\"].to_numpy()\n    ys = cfg[\"y_float\"].to_numpy()\n    n  = len(xs)\n\n    if n <= 1:\n        continue\n\n    diff = xs[:,None] - xs[None,:]\n    diffy= ys[:,None] - ys[None,:]\n    dist2 = diff*diff + diffy*diffy\n    np.fill_diagonal(dist2, np.inf)\n\n    min_dist = np.sqrt(np.min(dist2))\n    if min_dist < config_min_dist[cid]:   # baseline min dist\n        bad_configs.append((cid, min_dist, config_min_dist[cid]))\n\nbad_configs\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-18T23:05:06.785132Z","iopub.execute_input":"2025-11-18T23:05:06.785451Z","iopub.status.idle":"2025-11-18T23:05:06.841526Z","shell.execute_reply.started":"2025-11-18T23:05:06.785430Z","shell.execute_reply":"2025-11-18T23:05:06.840695Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================\n# CHUNK 6: Convert to submission format & save\n# =========================================\n\ndef float_to_s_array(arr: np.ndarray) -> np.ndarray:\n    arr = np.round(arr.astype(float), 6).astype(str)\n    return np.char.add(\"s\", arr)\n\nsubmission = improved_df.copy()\n\nsubmission[\"x\"]   = float_to_s_array(submission[\"x_float\"].to_numpy())\nsubmission[\"y\"]   = float_to_s_array(submission[\"y_float\"].to_numpy())\nsubmission[\"deg\"] = float_to_s_array(submission[\"deg_float\"].to_numpy())\n\nsubmission = submission[[\"id\", \"x\", \"y\", \"deg\"]]\n\nsubmission.to_csv(OUTPUT_FILE, index=False)\nprint(\"Saved submission to:\", OUTPUT_FILE)\n\nsubmission.head()\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-18T23:05:10.464777Z","iopub.execute_input":"2025-11-18T23:05:10.465064Z","iopub.status.idle":"2025-11-18T23:05:10.631155Z","shell.execute_reply.started":"2025-11-18T23:05:10.465041Z","shell.execute_reply":"2025-11-18T23:05:10.630195Z"}},"outputs":[],"execution_count":null}]}